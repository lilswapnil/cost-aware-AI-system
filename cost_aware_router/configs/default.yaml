pricing:
  path: cost_aware_router/pricing/pricing.yaml

cache:
  path: artifacts/prompt_cache.sqlite

cheap_model:
  tokenizer_path: artifacts/tokenizer/llm_spm.model
  checkpoint_path: artifacts/checkpoints/latest.pt
  device: cpu
  max_new_tokens: 128
  temperature: 0.0

openai:
  model: gpt-4o-mini
  timeout_s: 30

router:
  max_cost_usd: 0.02
  max_latency_ms: 2000
  min_quality: 0.6
